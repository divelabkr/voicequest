# 비밀키 — 절대 커밋 금지 (CLAUDE.md §9)
.env
.env.local
.env.*.local

# 의존성·빌드 산출물
node_modules/
dist/
.turbo/
*.tsbuildinfo

# 로그·임시
*.log
.DS_Store

# 콘텐츠 캐시 산출물(빌드타임 생성물 — 소스 아님)
content_cache/

# 런타임 상태(계정·세션·이벤트 — 테스트 데이터, 재생성 가능)
data/
